import {Dimensions, View, Text} from 'react-native';

import React from 'react';

const {width} = Dimensions.get('screen');

const RoomSearchScreen = () => {
  return (
    <View>
      <Text>ROOM SEARCH SCREEN</Text>
    </View>
  );
};

export default RoomSearchScreen;
